parameters:
    level: 8
    paths:
        - lib
    # Only the hand-written facade is analysed. src/ is generated by Kiota (never
    # hand-edited); tests fight Guzzle's loosely-typed history middleware and are
    # verified by being run, not type-checked.
